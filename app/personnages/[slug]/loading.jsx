export default function Loading() {
  return (
    <main className="home-page character-page">
      <section className="character-hero">
        <div className="character-hero-card">
          <p className="card-role">Chargement</p>
          <h1>Ouverture de la fiche personnage...</h1>
          <p className="character-summary">
            La page se prepare pour une navigation plus fluide entre les personnages.
          </p>
        </div>
      </section>
    </main>
  );
}
